export const GAME_ROWS: number = 10;
export const ROW_SIZE: number = 4;
export const DEFAULT_CELL_VALUE = -1;

// Six possible colours
export const PIN_VALUES: number[] = Array(6).fill(null).map((_, i) => i);